#include "push_swap.h"

int error_checker(t_args *args)
{
    int i;
    int base;

    i = 0;
    base = 0;

    if (arg[i] > INT_MAX || arg[i] < INT_MIN)
        printf("Error\n");
    if (arg[i] != ft_isdigit(i))
        printf("Error\n");
    if (!arg[i])
        prinf("Error\n")
    
}
